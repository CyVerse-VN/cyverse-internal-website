# Server-only backend URL, including the API version prefix.
# Next.js Server Actions use this value; browser JavaScript cannot access it.
BACKEND_API_URL=http://localhost:8000/api/v1

# Canonical frontend URL used to build absolute social-preview metadata.
APP_URL=http://localhost:3000
