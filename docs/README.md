# Tài liệu dự án

Thư mục này là nguồn tài liệu chuẩn cho yêu cầu, kiến trúc, API, bảo mật và triển khai.

Bắt đầu với `technology-baseline.md` để dùng đúng phiên bản runtime, framework, package manager và công cụ kiểm thử.
